version: 2

updates:
  # Keep the SHA-pinned GitHub Actions current. Grouped into one PR per run
  # to avoid churn; the trailing `# vX.Y.Z` comments are updated automatically.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - '*'
