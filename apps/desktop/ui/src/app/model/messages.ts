/** Shown when the user tries to apply a plan that no longer matches the selected game. */
export const STALE_PLAN_MESSAGE =
  'The selected operation plan is no longer current. Rebuild the plan before applying it.';
