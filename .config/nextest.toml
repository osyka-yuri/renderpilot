nextest-version = "0.9.143"

[profile.ci]
# Complete every test so one failure does not hide independent regressions.
fail-fast = false
# A flaky pass must not turn a failing attempt into a green quality gate.
retries = 0
